import React from 'react';
import PropTypes from 'prop-types';
import './Menus.css';

const Menus = () => (
  <div className="Menus" data-testid="Menus">
    Menus Component
  </div>
);

Menus.propTypes = {};

Menus.defaultProps = {};

export default Menus;
