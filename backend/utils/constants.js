
const PRIVILEGES = {
    read: "R",
    readWrite: "RW",
    none: "None"
}

const ERRORMSG = {
    invalidUrl: "Invalid URL Parameter",
    noPrivilege: "You have no privilege to do that"
}
module.exports = { PRIVILEGES, ERRORMSG }